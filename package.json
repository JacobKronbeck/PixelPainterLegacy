{
  "private": true,
  "scripts": {
    "build": "npm run build --prefix mytestvueapp.client",
    "dev": "npm run dev --prefix mytestvueapp.client",
    "preview": "npm run preview --prefix mytestvueapp.client"
  },
  "engines": {
    "node": "24.x"
  }
}
